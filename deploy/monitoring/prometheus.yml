global:
  scrape_interval: 30s
  evaluation_interval: 30s
scrape_configs:
  - job_name: slurm-insights-exporter
    static_configs:
      - targets: ["exporter:9341"]
rule_files:
  - /etc/prometheus/rules/slurm.yaml
